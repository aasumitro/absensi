import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.aasumitro.absensi',
  appName: 'absensi',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
